{
  "name": "messengar",
  "version": "1.0.0",
  "description": "WhatsApp CLI client for personal messaging with TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "tsc && node dist/main.js",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "daemon:start": "npm run build && node dist/main.js &",
    "daemon:stop": "pkill -f 'node dist/main.js'",
    "daemon:restart": "npm run daemon:stop && sleep 2 && npm run daemon:start",
    "daemon:status": "if [ -f messengar-daemon.pid ]; then pid=$(cat messengar-daemon.pid); if ps -p $pid > /dev/null 2>&1; then echo '✅ Daemon is running (PID: '$pid')'; else echo '❌ Daemon is not running (stale PID file)'; rm messengar-daemon.pid; fi; else echo '❌ Daemon is not running (no PID file)'; fi",
    "daemon:logs": "tail -n 50 messengar-daemon.log",
    "daemon:tail": "tail -f messengar-daemon.log"
  },
  "keywords": [
    "whatsapp",
    "cli",
    "typescript",
    "messaging",
    "notifications"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "js-yaml": "^4.1.1",
    "qrcode": "^1.5.4",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7",
    "whatsapp-web.js": "^1.34.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.2.3",
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "tar": "^7.0.0"
  }
}